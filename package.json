{
  "name": "ts-indian-rummy",
  "version": "0.0.1",
  "description": "Typescript library for Indian Rummy Card game",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cp package.json README.md ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/assert": "^1.5.6",
    "@types/debug": "^4.1.7",
    "@types/ramda": "^0.28.15",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "assert": "^2.0.0",
    "debug": "^4.3.4",
    "ramda": "^0.28.0"
  }
}
